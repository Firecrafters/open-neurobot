# Contributing

Contibutions are welcome, but remember to run `ignore-db-changes.sh` or manually run `git update-index --assume-unchanged data/bot_data.sqlite` to prevent any changes to the database from being committed.
